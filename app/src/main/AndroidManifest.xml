<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Kotlin_crud_1013">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".login.Sign"
            android:exported="true" />
        <activity
            android:name=".ManagmentPage.managment_page"
            android:exported="true" /> <!--액티비티 추가시 <application> 안에 추가해줘야함-->
        <activity
            android:name=".ManagmentPage.TodoService.Todo"
            android:exported="true" /> <!--액티비티 추가시 <application> 안에 추가해줘야함-->
        <activity
            android:name=".ManagmentPage.TodoService.TodoList"
            android:exported="true" /> <!--액티비티 추가시 <application> 안에 추가해줘야함-->

    </application>

</manifest>